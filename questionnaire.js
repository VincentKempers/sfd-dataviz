let questionnaire = (function() {
  return {
    init() {
      let allAnwsers = document.querySelector(".answers").children;
      let htmlValues = Object.values(allAnwsers);
      console.log(htmlValues);
      let chosenAnswers = [];
    }
  };
})();
